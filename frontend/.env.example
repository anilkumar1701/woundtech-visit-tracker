# Optional. Same-origin "/api" works via the Vite dev proxy and the nginx
# proxy in Docker (vite.config.ts / nginx.conf). Uncomment only if the UI
# should call a different API host.
# VITE_API_URL=http://localhost:4000/api
